const technologies = [  {    name: "Welding Robot",    category: "Welding",    capex: 260000000,    annualOpex: 35000000,    laborSaving: 90000000,    scheduleBenefit: 45000000,    reworkReduction: 20000000,    otherBenefit: 10000000  },  {    name: "Rebar Cage Automation",    category: "Civil",    capex: 320000000,    annualOpex: 42000000,    laborSaving: 120000000,    scheduleBenefit: 60000000,    reworkReduction: 25000000,    otherBenefit: 15000000  },  {    name: "3D Concrete Printing",    category: "Civil",    capex: 450000000,    annualOpex: 60000000,    laborSaving: 140000000,    scheduleBenefit: 90000000,    reworkReduction: 30000000,    otherBenefit: 20000000  },  {    name: "AGV / Mobile Transporter",    category: "Logistics",    capex: 180000000,    annualOpex: 28000000,    laborSaving: 70000000,    scheduleBenefit: 30000000,    reworkReduction: 10000000,    otherBenefit: 15000000  },  {    name: "Tank Coating Robot",    category: "Painting",    capex: 220000000,    annualOpex: 30000000,    laborSaving: 85000000,    scheduleBenefit: 35000000,    reworkReduction: 18000000,    otherBenefit: 12000000  }];function calculateAnnualGrossBenefit(item) {  return (    item.laborSaving +    item.scheduleBenefit +    item.reworkReduction +    item.otherBenefit  );}function calculateAnnualNetBenefit(item) {  return calculateAnnualGrossBenefit(item) - item.annualOpex;}function calculatePaybackPeriod(item) {  const annualNetBenefit = calculateAnnualNetBenefit(item);  if (annualNetBenefit <= 0) {    return null;  }  return item.capex / annualNetBenefit;}function determinePriority(item) {  const payback = calculatePaybackPeriod(item);  const annualNetBenefit = calculateAnnualNetBenefit(item);  if (payback === null) {    return "Low";  }  if (payback <= 3 && annualNetBenefit >= 100000000) {    return "High";  }  if (payback <= 5) {    return "Medium";  }  return "Low";}function formatCurrency(value) {  return new Intl.NumberFormat("ko-KR", {    style: "currency",    currency: "KRW",    maximumFractionDigits: 0  }).format(value);}function formatPayback(value) {  if (value === null) {    return "Not recoverable";  }  return `${value.toFixed(1)} years`;}function getPriorityClass(priority) {  if (priority === "High") {    return "priority-high";  }  if (priority === "Medium") {    return "priority-medium";  }  return "priority-low";}function renderTable() {  const tableBody = document.getElementById("resultsTable");  tableBody.innerHTML = "";  technologies.forEach((item) => {    const annualGrossBenefit = calculateAnnualGrossBenefit(item);    const annualNetBenefit = calculateAnnualNetBenefit(item);    const payback = calculatePaybackPeriod(item);    const priority = determinePriority(item);    const row = document.createElement("tr");    row.innerHTML = `      <td>${item.name}</td>      <td>${item.category}</td>      <td>${formatCurrency(item.capex)}</td>      <td>${formatCurrency(annualGrossBenefit)}</td>      <td>${formatCurrency(item.annualOpex)}</td>      <td>${formatCurrency(annualNetBenefit)}</td>      <td>${formatPayback(payback)}</td>      <td class="${getPriorityClass(priority)}">${priority}</td>    `;    tableBody.appendChild(row);  });}function renderSummary() {  const totalCount = document.getElementById("totalCount");  const bestPayback = document.getElementById("bestPayback");  const highestBenefit = document.getElementById("highestBenefit");  totalCount.textContent = technologies.length;  const recoverableTechnologies = technologies    .map((item) => ({      name: item.name,      payback: calculatePaybackPeriod(item)    }))    .filter((item) => item.payback !== null);  if (recoverableTechnologies.length === 0) {    bestPayback.textContent = "-";  } else {    const best = recoverableTechnologies.reduce((currentBest, item) =>      item.payback < currentBest.payback ? item : currentBest    );    bestPayback.textContent = `${best.name} (${best.payback.toFixed(1)} years)`;  }  const bestBenefitItem = technologies.reduce((currentBest, item) =>    calculateAnnualNetBenefit(item) > calculateAnnualNetBenefit(currentBest)      ? item      : currentBest  );  highestBenefit.textContent = `${bestBenefitItem.name} (${formatCurrency(    calculateAnnualNetBenefit(bestBenefitItem)  )})`;}function renderApp() {  renderTable();  renderSummary();}function getNumberInputValue(id) {  const value = Number(document.getElementById(id).value);  if (Number.isNaN(value) || value < 0) {    return 0;  }  return value;}document.getElementById("technologyForm").addEventListener("submit", (event) => {  event.preventDefault();  const newTechnology = {    name: document.getElementById("technologyName").value.trim(),    category: document.getElementById("category").value,    capex: getNumberInputValue("capex"),    annualOpex: getNumberInputValue("annualOpex"),    laborSaving: getNumberInputValue("laborSaving"),    scheduleBenefit: getNumberInputValue("scheduleBenefit"),    reworkReduction: getNumberInputValue("reworkReduction"),    otherBenefit: getNumberInputValue("otherBenefit")  };  technologies.push(newTechnology);  event.target.reset();  renderApp();});renderApp();
